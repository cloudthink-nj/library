package com.ibroadlink.library.base.utils

import java.util.concurrent.TimeUnit

/**
 * @Author: Broadlink lvzhaoyang
 * @CreateDate: 2021/3/25 18:20
 * @Email: zhaoyang.lv@broadlink.com.cn
 * @Description: 常量
 */
object BLConstantUtils {
    val ONE_SECOND_DURATION = TimeUnit.SECONDS.toMillis(1)
    val TWO_SECOND_DURATION = TimeUnit.SECONDS.toMillis(2)
    val FIVE_SECOND_DURATION = TimeUnit.SECONDS.toMillis(5)
    val TEN_SECOND_DURATION = TimeUnit.SECONDS.toMillis(10)
    val FIFTEEN_SECOND_DURATION = TimeUnit.SECONDS.toMillis(15)
    val TWENTY_SECOND_DURATION = TimeUnit.SECONDS.toMillis(20)
    val THIRTY_SECOND_DURATION = TimeUnit.SECONDS.toMillis(30)
    val ONE_MINUTE_DURATION = TimeUnit.MINUTES.toMillis(1)
    val TWO_MINUTE_DURATION = TimeUnit.MINUTES.toMillis(2)
    val FIVE_MINUTE_DURATION = TimeUnit.MINUTES.toMillis(5)
    val TEN_MINUTE_DURATION = TimeUnit.MINUTES.toMillis(10)
    val THIRTY_MINUTE_DURATION = TimeUnit.MINUTES.toMillis(30)
    val ONE_HOUR_DURATION = TimeUnit.HOURS.toMillis(1)
    val TWO_HOUR_DURATION = TimeUnit.HOURS.toMillis(2)
    val FOUR_HOUR_DURATION = TimeUnit.HOURS.toMillis(4)
    val HALF_DAY_DURATION = TimeUnit.HOURS.toMillis(12)
    val ONE_DAY_DURATION = TimeUnit.DAYS.toMillis(1)
    val THREE_DAY_DURATION = TimeUnit.DAYS.toMillis(3)
    val FIVE_DAY_DURATION = TimeUnit.DAYS.toMillis(5)
}